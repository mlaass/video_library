[tool.poetry]
name = "screen-recording"
version = "0.1.0"
description = ""
authors = ["Moritz Laass <moritz.laass@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
